Job Tracker Frontend

This is a simple React frontend for the Job Tracker backend.

Prerequisites
- Node.js (14+ recommended)

Install
- cd frontend/job-tracker-frontend
- npm install

Run
- npm start

Notes
- Backend must run at http://localhost:8080
- The app uses Axios to call the backend API at /jobs
